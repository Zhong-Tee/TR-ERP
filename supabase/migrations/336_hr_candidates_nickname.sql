-- =============================================================================
-- เพิ่มคอลัมน์ชื่อเล่นให้ผู้สมัคร
--   หน้า "นัดสัมภาษณ์" มีช่อง/คอลัมน์ "ชื่อเล่น" มาตั้งแต่ต้น แต่ hr_candidates
--   ไม่เคยมีคอลัมน์นี้ → บันทึกแล้วขึ้น "Could not find the 'nickname' column"
-- IDEMPOTENT: safe to re-run
-- =============================================================================

ALTER TABLE hr_candidates ADD COLUMN IF NOT EXISTS nickname TEXT;
