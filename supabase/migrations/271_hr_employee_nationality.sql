-- =============================================================================
-- เพิ่มคอลัมน์ สัญชาติ (nationality) ให้ตารางพนักงาน
--   - ใช้ในฟอร์มแก้ไขพนักงาน แถวเดียวกับ เพศ / ศาสนา
--   - ที่อยู่ตามบัตรประชาชน = address, ที่อยู่ปัจจุบัน = current_address (มีอยู่แล้ว)
-- IDEMPOTENT: รันซ้ำได้
-- =============================================================================

ALTER TABLE hr_employees ADD COLUMN IF NOT EXISTS nationality TEXT;
