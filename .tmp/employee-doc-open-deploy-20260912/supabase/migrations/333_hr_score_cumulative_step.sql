-- =============================================================================
-- คะแนนการปฏิบัติงาน — กติกาสะสมแบบ "เพิ่มขึ้นทุกครั้ง"
--
-- เดิม: เกินโควตาแล้วหักเท่ากันทุกครั้ง (points ค่าเดียว)
-- ใหม่: หักเพิ่มขึ้นทีละ points_step ในครั้งที่เกินถัดไป
--       ครั้งที่เกินลำดับที่ n หัก = points + points_step × (n − 1)
--
--       ตัวอย่าง: ยอมให้ 5 ครั้ง · points -2 · points_step -2
--         สาย 6 ครั้ง → -2
--         สาย 7 ครั้ง → -2, -4
--         สาย 8 ครั้ง → -2, -4, -6
--
-- points_step = 0 (ค่าเริ่มต้น) = พฤติกรรมเดิมเป๊ะ ๆ กติกาที่ตั้งไว้แล้วไม่กระทบ
--
-- IDEMPOTENT: safe to re-run
-- =============================================================================

ALTER TABLE hr_score_rules
  ADD COLUMN IF NOT EXISTS points_step NUMERIC(6,2) NOT NULL DEFAULT 0;

COMMENT ON COLUMN hr_score_rules.points_step IS
  'กติกาสะสมเท่านั้น: หักเพิ่มทีละเท่านี้ในครั้งที่เกินถัดไป (0 = หักเท่ากันทุกครั้ง) · ครั้งที่ n หัก = points + points_step × (n-1)';

NOTIFY pgrst, 'reload schema';
